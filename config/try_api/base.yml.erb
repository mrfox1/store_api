name: 'StoreApi'
host: <%= ENV['API_HOST'] || "http://localhost:3000"%>
api_prefix: '/api/v1'

include!: variables

menu_items:
  -
    include!: categories
  -
    include!: sessions
  -
    include!: users
